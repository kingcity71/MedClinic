@* PropName, LabelValue, PropValue, IsValidation, inputType *@
@model (string,string, string,bool, string)
    <div class="form-group row p-1">
        <label name="@Model.Item1" class="col-sm-3 col-form-label" style="text-wrap:normal">@Model.Item2</label>
        <div class="col-sm-9">
            <input type="@Model.Item5" name="@Model.Item1" class="form-control" value="@Model.Item3">
        </div>
        @if (Model.Item4)
        {
            <span asp-validation-for="@Model.Item1" />}

    </div>